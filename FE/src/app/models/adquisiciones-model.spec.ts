import { AdquisicionesModel } from './adquisiciones-model';

describe('AdquisicionesModel', () => {
  it('should create an instance', () => {
    expect(new AdquisicionesModel()).toBeTruthy();
  });
});
